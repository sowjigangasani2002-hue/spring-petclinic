name: Java-workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    name: my-spec
    runs-on: self-hosted

    steps:
      - name: Git checkout
        uses: actions/checkout@v4

      - name: Build Java with Maven
        run: mvn package        
      