name: Java-workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    name: my-spec
    runs-on: ubuntu-latest

    steps:
      - name: Git checkout
        uses: actions/checkout@v4

      - name: Build Java with Maven
        run: mvn package
        
      - name: SonarQube Scan

        
        


        
      
        

